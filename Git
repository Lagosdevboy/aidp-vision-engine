# make sure you're on main
git checkout main
git pull origin main

# create a new branch
git checkout -b feat/aidp-gpu-integration

# add your changes
git add .
git commit -m "Add GPU-accelerated market intelligence using AIDP"

# push the branch
git push origin feat/aidp-gpu-integration